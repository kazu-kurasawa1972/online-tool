# 平文のローカル作業用データ（暗号化前の中身）はコミットしない
data.json

# 年度・学期ごとのバックアップ（data.2026-zenki.json など）も平文なのでコミットしない
data.*.json

# ただし中身が空のテンプレートは公開してよい
!data.example.json

# QRコードの元画像を置く作業用フォルダ。
# ここの画像は data.json に埋め込んで暗号化するため、そのままでは公開しない
# （リポジトリに置くと誰でも直接ダウンロードできてしまう）
qr-source/

# 公開ページが読むのは暗号化済みの data.enc のみ（こちらはコミットする）
